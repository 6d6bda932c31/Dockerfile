apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: ubuntu
        uses: docker://ubuntu:22.04
        shell: sh
        run: |
          apt-get update
